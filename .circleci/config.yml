version: 2.1

commands:
  store_artifact:
    parameters:
      target:
        type: string
      exec:
        type: string
    steps:
      - store_artifacts:
          path: ./pkg/shift-code-<< parameters.exec >>
executors:
  node:
    docker:
      - image: circleci/node:lts

jobs:
  install:
    executor: node
    steps:
      - checkout
      - run:
          name: Install
          command: npm ci

      - persist_to_workspace:
          root: .
          paths:
            - node_modules

  build:
    executor: node
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          name: Build
          command: npm run build
      - run:
          name: Check
          command: ls -la dist
      - persist_to_workspace:
          root: .
          paths:
            - dist

  artifact:
    executor: node
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          name: Package
          command: npm run pkg
      - run:
          name: Check
          command: ls -la pkg
      - store_artifact:
          target: linux
          exec: linux
      - store_artifact:
          target: mac
          exec: macos
      - store_artifact:
          target: windows
          exec: win.exe
      - persist_to_workspace:
          root: .
          paths:
            - pkg

  publish:
    executor: node
    steps:
      - checkout
      - attach_workspace:
          at: .
      - run:
          name: Publish
          command: npx semantic-release

workflows:
  version: 2

  publish:
    jobs:
      - install
      - build:
          requires:
            - install
      - artifact:
          requires:
            - build
      - publish:
          requires:
            - artifact
          filters:
            branches:
              only: master